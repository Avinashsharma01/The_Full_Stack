<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8" />
        <meta name="viewport" content="width=device-width, initial-scale=1.0" />
        <title>Forgot Password - <%= appName %></title>
        <link rel="stylesheet" href="./forgot-password.css" />
    </head>
    <body>
        <div class="container">
            <h1>Forgot Password</h1>
            <p class="subtitle">
                Enter your email address and we'll send you a link to reset your
                password
            </p>

            <div class="info-box">
                📧 We'll send you a secure reset link that expires in 15 minutes
            </div>

            <% if (error) { %>
            <div class="message error"><%= error %></div>
            <% } %> <% if (success) { %>
            <div class="message success"><%= success %></div>
            <% } else { %>
            <form id="forgotForm">
                <div class="form-group">
                    <label for="email">Email Address:</label>
                    <input
                        type="email"
                        id="email"
                        name="email"
                        required
                        placeholder="Enter your registered email address"
                        value="<%= email || '' %>"
                    />
                </div>

                <button type="submit" id="submitBtn">Send Reset Link</button>

                <div class="loading" id="loading">
                    <div class="spinner"></div>
                    <p>Sending reset link...</p>
                </div>
            </form>
            <% } %>

            <div id="message"></div>

            <div class="back-to-login">
                <a href="/">← Back to Home</a>
            </div>
        </div>

        <script>
            document
                .getElementById("forgotForm")
                ?.addEventListener("submit", async function (e) {
                    e.preventDefault();

                    const email = document.getElementById("email").value;
                    const messageDiv = document.getElementById("message");
                    const submitBtn = document.getElementById("submitBtn");
                    const loading = document.getElementById("loading");

                    // Clear previous messages
                    messageDiv.innerHTML = "";

                    // Validate email
                    if (!email) {
                        messageDiv.innerHTML =
                            '<div class="message error">Please enter your email address</div>';
                        return;
                    }

                    // Basic email validation
                    const emailRegex = /^[^\s@]+@[^\s@]+\.[^\s@]+$/;
                    if (!emailRegex.test(email)) {
                        messageDiv.innerHTML =
                            '<div class="message error">Please enter a valid email address</div>';
                        return;
                    }

                    // Show loading state
                    submitBtn.disabled = true;
                    loading.style.display = "block";

                    try {
                        const response = await fetch(
                            "/api/v1/user/forgotPass",
                            {
                                method: "POST",
                                headers: {
                                    "Content-Type": "application/json",
                                },
                                body: JSON.stringify({ email: email }),
                            }
                        );

                        const data = await response.json();

                        if (response.ok) {
                            messageDiv.innerHTML =
                                '<div class="message success">Reset link sent successfully! Please check your email inbox.</div>';
                            document.getElementById(
                                "forgotForm"
                            ).style.display = "none";
                        } else {
                            messageDiv.innerHTML = `<div class="message error">${
                                data.message || "Error sending reset link"
                            }</div>`;
                        }
                    } catch (error) {
                        messageDiv.innerHTML =
                            '<div class="message error">Network error. Please try again.</div>';
                    } finally {
                        // Hide loading state
                        submitBtn.disabled = false;
                        loading.style.display = "none";
                    }
                });

            // Auto-focus on email field
            document.getElementById("email")?.focus();
        </script>
    </body>
</html>
